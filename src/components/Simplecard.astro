---
interface Props {
	href: string;
    alt:string;
    src: string;
	title: string;
	body: string;
    
    gradient: string;
}

const { href, title, body ,alt ,src,gradient} = Astro.props;

import Bagde from './Bagde.astro';
---

<li class="link-card">
    <a href={href} class="card" style={`--card-gradient: ${gradient}`}>
        <img class="card__image" src={src} alt={alt} />
        <div class="card__content">
            <h2 class="card__title">{title}</h2>
            <p class="card__text">
            {body}
            </p>
            
        </div>
    </a>
</li>

<style>
.link-card {
		list-style: none;
		display: flex;
		padding: 1px;
		background-color: var(--color-ligth);
		background-image: none;
		background-size: 400%;
		border-radius: 1rem;
		background-position: 100%;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: inset 0 0 0 1px var(--color-grey);
	}    


.card {
		width: 100%;
		text-decoration: none;
		line-height: 1.4;
		padding: calc(1.5rem - 1px);
		border-radius: 1rem;
		color: var(--color-light);
		background-color: var(--color-ligth);
		opacity: 0.8;
	}

/* Pseudo-elemento para el borde degradado */
/* .card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 1rem;
    padding: 0.25rem;
    background: var(--color-dark);
    background: var(--card-gradient); 
    -webkit-mask: 
        linear-gradient(var(--color-ligth) 0 0) content-box, 
        linear-gradient(var(--color-ligth) 0 0);
    -webkit-mask-composite: xor;
            mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s;
}
*/

/* Mostrar el borde degradado al hacer hover */
/* .card:hover::before {
    opacity: 1;
} */
/* Imagen */
.card__image {
    width: 100%;
    display: block;
}

/* Contenido */
.card__content {
    padding: 1.25rem;
}


/* Título */
.card__title {
    margin-bottom: 0.5rem;
    font-weight: 700;
    letter-spacing: -0.025em;
    color: var(--color-dark);
	font-size: 1rem;
    line-height: 2rem;
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
	/* font-family: Biotif, sans-serif; */
    font-family: "Poppins", sans-serif;
}

/* Texto */
.card__text {
    margin-bottom: 0.75rem;
    font-weight: 300;
    color: var(--color-dark);
	font-size: 1rem;
    line-height: 2rem;
	/* font-family: Biotif, sans-serif; */
    font-family: "Poppins", sans-serif;
}

.link-card:is(:hover, :focus-within) {
		background-position: 0;
		background-image: var(--gradient-green-yellow);
	}
.link-card:is(:hover, :focus-within) .card__title {
		color: #4ea87d;
	}
</style>